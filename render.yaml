services:
  - type: web
    name: rag-chatbot-backend
    runtime: docker
    plan: free
    rootDir: backend
    envVars:
      - key: COHERE_API_KEY
        sync: false
      - key: QDRANT_URL
        sync: false
      - key: QDRANT_API_KEY
        sync: false
